name: faro_sdk
description: Faro SDK for Flutter / Dart — sends logs and captured exceptions to a Faro instance.
version: 0.1.0
homepage: https://github.com/iaportafolio/faro
environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"
dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
dev_dependencies:
  flutter_test:
    sdk: flutter
